# Python Rules

- Prefer typed Python where practical.
- Keep side effects at the edges.
- Use small modules with clear responsibilities.
- Prefer explicit interfaces over global state.
- Use pytest for tests when available.
- Prefer ruff for linting/formatting when the project already uses it.
- Add tests for bug fixes and conditional logic.