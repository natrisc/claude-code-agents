# Rust Rules

- Prefer explicit public APIs.
- Keep ownership and borrowing decisions readable.
- Avoid unnecessary cloning.
- Keep `cargo fmt` and `cargo clippy` clean where configured.
- Document unsafe code or performance-sensitive trade-offs.
- Add focused tests for new logic and bug fixes.